# SEO/GEO 月度自動稽核 — 護欄

> 給每月 1 號自動跑的雲端 SEO/GEO 稽核 agent 看的。這份是精簡版，正本
> （完整施工規則、內容規則、踩雷紀錄）在站主本機的 `RUNBOOK.md` 與
> `loop_plan.md`，這兩份**不在這個 git repo 裡**，雲端 agent 讀不到，所以
> 這裡把跟自動化最相關的規則抄一份。跟正本衝突時，以「已經上線的實際內容」
> 為準——線上頁面就是事實來源，不要因為這份文件過時而改壞已經對的東西。

## 絕對不能做（違反就停手，改成回報而非動手）

1. **不虛構事實**：年份、媒材、尺寸、場地、策展人、日期——網站上已有的文字
   查不到就跳過該欄位，不要用猜的、不要用「合理推測」補。
2. **不推論作品歸屬**：某作品屬於哪個系列，只能引用網站上已經寫的敘述，
   不能從媒材或外觀相似自己推斷（曾經發生過把《長大》誤標成別的系列）。
3. **不為 SEO 改寫**：作品題名、展覽原題、藝術家自己的論述文字——逐字保留，
   不准為了關鍵字密度或可讀性去動。第三方報導標題（如新聞稿）引用時也要
   逐字，不能「校正」用字。
4. **不改 URL 結構**（頁面路徑），**不刪除既有內容**。圖片檔名重新命名例外
   （已經做過一輪，見下方「已完成」），但改檔名要同步全部三語 HTML 引用，
   一個都不能漏，動手前後都要跑一次「引用檔案是否都存在」的檢查。
5. **不臆造版權／授權條款**：不要自己幫圖片加 `license`／`copyrightNotice`
   之類的 schema.org 欄位——這是站主的法律決定，不是技術決定。
6. **繁體中文，臺灣用字，全形標點**（中文句子）；「臺灣」「臺北」等地名用
   「臺」不用「台」，除非是官方全稱本來就用「台」的專有名詞、或是逐字引用
   的展覽原題／第三方標題（那些不准動，見上第 3 點）。日文內容用日文自己的
   漢字寫法（会/写/国/湾/号/医/当 等），不要「校正」成繁體中文——這是兩種
   語言，不是錯字。

## 已完成，不用重做（跑之前先確認網站現況，如果已經是這樣就跳過）

- sitemap.xml：只收 36 個可索引 HTML 頁 + image sitemap extension（不要加回
  `okf/**/*.md` 或 `llms.txt`，那些是 text/markdown，加回去只會稀釋訊號）。
  重產用 `migration/gen_sitemap.py`（不在這個 repo，在站主本機的
  `../migration/`——如果雲端環境沒有這支腳本，直接手動維護 sitemap.xml 或
  重寫一支等效的，邏輯是：掃 `site/**/index.html`，收錄頁面 URL + 每頁
  `<img>` 的 `data-full`（優先）或 `src`，`alt=""` 的裝飾圖跳過）。
- 圖片檔名：`answer/`、`medicine/`、`home/`、`about/`、
  `exhibitions/subtropical-signals-singapore-2026/` 都已經改成語意檔名。
  `chaos-body-shop/` 的檔名本來就合格，不用動。
- JSON-LD：Person 共用實體（`#person`）＋各頁型別＋Breadcrumb，含
  `disambiguatingDescription`（三語）。
- OKF bundle（`site/okf/`）：三檔個展 + 兩檔聯展（觀和作用、亞熱帶訊號）都有
  完整資料夾；`llms.txt` 已登記所有 OKF 條目。

## 還沒做、需要站主決定，不要自己動（發現了就寫進報告，不要改）

- `works/answer` 的 JSON-LD `dateCreated: "2024–2025"` 不是合法 ISO-8601，
  但沒有 Google rich result 影響，需要站主選一個代表年份。
- `works/medicine` 的 hero／前兩段是意象語言，硬事實（如「78 張塔羅牌」）
  要到後段才出現在可見文字——這是藝術家自己的論述語氣，不要主動改寫。
- 幫代表圖加 `ImageObject`（license／creator／creditText）換 Google 圖片
  「可授權」標籤——這是版權宣告，見上方絕對不能做第 5 點。
- Google Search Console 驗證——這是站主帳號層級的操作，agent 做不到，
  每次稽核到這項提醒現況即可（例如：新站是否已被搜尋引擎索引、品牌詞
  benchmark 查詢新站是否出現），不用嘗試繞過。

## 這次稽核的方法

- 技術面用 `curl`／讀原始碼／`json.loads` 驗證，不要只看畫面。判斷舊站／
  外部連結死活一律用 GET（`curl -s -o /dev/null -w "%{http_code}"`），
  不要只用 HEAD（`-I`/`-sIL`）——有些主機（如 Adobe Portfolio／Fastly）對
  HEAD 一律回假的 404。
- 每次改動：改完 → 驗證（相對路徑檔案存在、JSON-LD 解析、sitemap 圖片數量
  對得上）→ commit（`<type>: <description>` 格式）→ push → 用 GET 輪詢上線
  確認 → 才算做完一項。
- 找到高槓桿問題就先修一項、驗證上線後再找下一項，不要一次改一大批不驗證。
- 收工前用 WebSearch 針對品牌詞（許哲睿／HSU Che-jui）加最近作品/展覽關鍵字
  各查一次，記錄新站有沒有開始出現在結果或 AI 摘要裡，寫進報告；如果一直
  沒有，那是索引時間／GSC 驗證的問題，不是技術問題，不要為了「讓它出現」
  去做本文件禁止的事（虛構、改題名、亂加結構化資料）。
